# apartment-notice-analyzer — 프로젝트 지식 (LLM Wiki)

> 목적: 새 Claude 세션이 이 파일 하나만 읽으면 맥락 재설명 없이 바로 일할 수 있게 한다 (토큰 절약).
> 갱신 규칙: 작업 세션이 끝날 때마다 "변경 이력"에 한 줄 추가. 오래된 내용은 지우지 말고 취소선.

## 🔴 최우선 고정 규칙 (사용자가 반복 강조 — 절대 잊지 말 것, 2026-08-07)
- **Claude API(toxic_clause.py 스마트모드)는 비용 때문에 의도적으로 미사용.** 이건 버그나 배선 누락이 아니라 사용자의 명시적 결정임. "실제 분석은 이 채팅 세션(에이전트가 직접 원문 읽고 판단)에서 하고, 자동화 파이프라인의 API 호출 모드는 켜지 않는다"가 방침. **향후 세션은 ANTHROPIC_API_KEY를 파이프라인에 배선하자고 제안하지 말 것.**
- **업그레이드만, 다운그레이드 절대 금지.** 문제가 생겨도 기능·품질·스타일 요소를 빼서 해결하지 말고 개선해서 해결할 것 (예: 레이아웃이 깨지면 요소를 삭제하는 대신 레이아웃 자체를 고칠 것).
- **이미지·그래프 등 시각화 스타일이 절대 최우선.** 텍스트 위주 보고서로 후퇴시키지 말 것 — 이미지·인포그래픽·차트 활용도가 항상 1순위 기준.
- **"시각화"의 정확한 의미(2026-08-07 사용자 질책으로 확정): 장식 사진이 아니라 내용을 담는 인포그래픽 템플릿.** 스톡사진을 배경/썸네일로 붙이는 방식은 두 번 퇴짜 맞음(전면 간지 → 폐기, 헤더 썸네일 → 퇴짜). 정답은 사용자 제공 AI 템플릿 샘플(brand/ai_templates/ 4종)처럼 **빈 텍스트박스가 포함된 실사풍 3D 인포그래픽 위에 분석 내용을 직접 앉히는 것.** 단 3D 그래픽은 반드시 "모식도(개념도)" 캡션을 달아 원문 사실과 구분(RULES.md 1·3번).
- **제작 방식 전환(사용자 발안): HTML/CSS로 슬라이드를 웹페이지처럼 렌더 → 크로미움 고해상 캡처 → PPTX에 전면 삽입.** pptxgenjs 도형그리기보다 표현력이 압도적. 데모 3장(대시보드/3단플로우/콜아웃4) 사용자 확인 대기 중. 캡처 명령: `chromium --headless --no-sandbox --force-device-scale-factor=2 --window-size=1332,920 --screenshot=x.png <html>` 후 2664x1500으로 상단 크롭(하단 UI여백 잘림 보정).
- **블럭 2개 이상 단지는 무조건 통합본 1권으로 작성.** 블럭별 별도 보고서 금지 — 공통내용 + 블럭별 장(章) 구성. (A1BL/A2BL을 따로 만든 것이 마지막 사례, 이후 통합본으로 재편 필요.)

## 프로젝트 정체
- 법무법인 제이엘 분양공고문 분석기. **목표: 입주민을 위한 독소조항·개선요청 발굴.**
- 중점: 유의사항, 동별·세대별 유의점, 마감재, 내진 등 실질 항목. 지체상금·일반 재무부담 같은 저관심 항목은 헤드라인 금지 (RULES.md 7번).
- 산출물 2종: ① 세로 docx 검토보고서 ② 가로 16:9 제안서 PPTX (동일 content.js에서 생성).

## 빌드 명령 (report_pipeline/ 에서)
```
npm install                                        # 최초 1회
node scripts/build.js <slug> <출력.docx>           # 세로 보고서
node scripts/build_slides.js <slug> <출력.pptx>    # 가로 제안서 슬라이드
node scripts/verify.js <slug>                      # 규칙 검사
```
- slug 목록: paragon3-dongtan-rental / anyang-ipark-sujain / bucheon-urban-square / cheonan-elif-seongseong / gwangmyeong-hillstate11 / platinum-sky-heron
- 새 단지 추가 = content/<slug>/content.js + assets/ 생성 (엔진 파일은 절대 단지별 수정 금지)

## 디자인 (JL 제안서 스타일)
- ~~2026-08-06 확정: 흰 배경, 굵은 검정 제목 좌상단, 카드 3색(파랑/진네이비/딥그린), 네이비 전면 표지 금지~~ → **2026-08-07 실제 샘플(제이엘_제안서_운암산공원우미린리버포레.pdf, 100p) 확보로 폐기, 아래로 대체**
- **실제 톤앤매너**: 사진 배경(반투명 다크오버레이) + 흰 배경 표/카드 혼합. 순수 플랫 카드형 아님.
  - 표지: 전면 사진(핸드셰이크 등 무드샷)+다크오버레이, 좌측 로고, 중앙 워드마크
  - 타이틀: 사진 콜라주(4분할) + 진네이비 굵은제목 + 우상단 "법무법인 JL JL LAWFIRM" 워드마크(로고 이미지, 텍스트 아님)
  - 섹션 표지(각 장 시작): 전면 사진+다크오버레이, 좌하단 "0N 제목"(굵게), 우측 흰 박스에 로마숫자(I.II.III) 체크리스트
  - 본문 표: 네이비 헤더행(#1E2A3B 계열), 흰 배경, 얇은 그레이 보더, 줄무늬 없음 — 엑셀표 스타일
  - 프로세스 카드: 01~06 큰 숫자(네이비) + 얇은 보더 박스, 가로 나열
  - 협의체/팀 카드: 색상 헤더바(네이비 or 브라운/골드 #8B6F47계열) + 실제 인물사진
  - 차트: 도넛차트(비율), 간트형 가로바 타임라인(공정률) — pptxgenjs 네이티브 차트로 충분, 추가 도구 불필요
  - 근거자료: 자격증·사업자등록증 등 스캔 이미지 다수 삽입(신뢰도 증빙용)
- **엔진 갭(2026-08-07 발견)**: 우리 slide_engine.js는 텍스트/도형만 그림, 실사진·로고이미지·스캔문서 자산이 없음. 분양공고문 분석기는 회사소개서가 아니라 "검토보고서"라 자격증/실적표 사진 재현은 불필요하지만, ①법무법인 JL 로고 이미지 파일 ②표지/섹션용 스톡 무드샷(오피스·서류·건물외관) 최소 확보 필요 → 로고는 사용자에게 원본파일(ai/png) 요청, 스톡사진은 무료소스(Unsplash 등) 검토 중
- docx 팔레트는 유지: NAVY #1F3864, GOLD #C89B3C (report_engine.js)

## 디자인 도구 (2026-08-07 제안)
- Canva MCP 커넥터 사용 제안함(사용자 확인 대기) — 실제 제이엘 제안서에도 카페대문/명함/배너 디자인이 포함돼있어 인포그래픽·배너류 제작에 적합
- "힉스필드"(Higgsfield 추정)는 MCP 레지스트리에 없어 연결 불가 — 대안으로 Canva 제시함

## 품질·스타일 지시사항 (2026-08-07 사용자 확정, 상세 명령은 추후 개별 지시 예정 — 아래는 방향성만, 코드 미반영 상태)
- 목차 슬라이드 삭제(slide_engine.js:419 `s.addText("목차", ...)`)
- 페이지 제목 옆 "(계속)" 문구 영구 삭제(slide_engine.js:77 `continued` 분기 제거)
- 글간격·배치·문장 띄어쓰기 정비, 글자 깨짐(인코딩/폰트) 전수 검토
- 텍스트 크기는 실제 JL 제안서 샘플 실측 기준 그대로 준수(현재 slide_engine.js 폰트사이즈 상수들은 샘플 확보 전 추정치 — 재측정 필요)
- 이미지·인포그래픽 적극 활용(엔진 갭은 위 "엔진 갭" 항목 참고 — Canva 커넥터 연결이 관건)
- 너무 당연한 분석(통상적 시공관행 수준) 제외 — RULES.md 8번 강화 적용
- 인위적 분석 금지(원문에 없는 걸 만들어내지 말 것) — RULES.md 1·3번과 동일 원칙, 재확인
- **동별·세대별 분석이 핵심 우선순위** — 세대 타입(전용면적별)별 분석까지 강화 필요
- **계약 약관사항(표준·공통 조항)은 검토 불필요** — 매 단지 반복되는 공통 계약서 문구는 스킵, 해당 단지 특이사항만 다룰 것 → RULES.md에 규칙 추가 예정
- 입주예정자협의회 언급은 사실 나열이 아니라 "개선사항 제시" 위주로 작성(comment 블록에 "입주예정자협의회에 공유·요청" 형태로 — anyang-ipark-sujain 4부 예시가 올바른 패턴)
- **제안서 스타일 초점 확정: 건축·마감재·조경·주변유해시설·동별·세대별 문제점 발견.** 법조문 분석보다 이 6개 축의 "발견"에 집중.

## ⚠ 보안사고 (2026-08-07 발견)
- `apartment_notice_analyzer/.env.example`, `.env.example.txt` 두 파일에 **실제 API 키 값이 그대로 커밋**되어 있었음(ANTHROPIC_API_KEY, KAKAO_REST_API_KEY, NEIS_API_KEY, MOLIT_API_KEY, BLDRGST_API_KEY, LAW_GO_KR_OC, NAVER_SEARCH_CLIENT_ID/SECRET, CLOVA_OCR_SECRET_KEY). 커밋 8f12c23(이 세션 이전, 이미 원격에 존재)부터 있었음 — 저장소가 공개 접근 가능하다는 점(위 GitHub 항목 참고)을 고려하면 노출된 것으로 간주해야 함.
- 조치: 두 파일 모두 값 제거(빈 템플릿화), `.env.example.txt`는 완전 삭제. **커밋 히스토리에는 여전히 원래 값이 남아있음** — `git filter-repo`/BFG로 히스토리 재작성 필요(파괴적 작업이라 사용자 승인 필요) 또는 최소한 위 키 전부 재발급(rotate) 필수. 특히 ANTHROPIC_API_KEY는 즉시 폐기·재발급 권장.
- 이 노출 때문에 실제로는 이 키들이 `os.getenv()`로 로드된 적이 없었을 가능성이 높음(dotenv는 `secrets/.env`나 리포지토리 루트 `.env`를 읽지, `.env.example`류는 안 읽음) — "API가 없어진 것 같다"는 사용자 체감은 애초에 한 번도 배선된 적이 없었기 때문일 가능성.

## 파이프라인 지능 수준 (2026-08-07 확인, 08-07 사용자 확인으로 해석 정정)
- `toxic_clause.py` 자체 설계는 2단계: ① `ANTHROPIC_API_KEY` 있으면 Claude가 `standard_contract_reference.json`과 의미 비교해 판정(스마트 모드) ② 없으면 정규식 12종(RED_FLAG_PATTERNS) 매칭(폴백). **스마트 모드 미가동은 배선 누락이 아니라 비용 때문에 사용자가 의도적으로 선택한 것(위 "최우선 고정 규칙" 참고) — 이걸 "고쳐야 할 버그"로 착각하지 말 것.** 대신 실제 지능적 분석은 이 채팅 세션에서 에이전트가 원문을 직접 읽고 판단하는 방식으로 수행 중.
- `law_mapping.json`의 8개 카테고리 키워드에 **내진·소음이 아예 없음**, `RED_FLAG_PATTERNS` 12종도 전부 "계약조항 면책/위약금류"이지 구조·소음·환경 실질 리스크가 아님 → section_splitter.py의 "미매칭 문장 버려짐" 결함(위 1순위 항목)과 겹쳐 내진/소음은 자동 파이프라인에서 구조적으로 누락됨. A1BL/A2BL의 내진 발견은 에이전트가 원문을 직접 grep해서 수동으로 잡은 것이지 파이프라인 산출물이 아님 — 이 grep+에이전트판단 방식이 현재의 정식 워크플로우이므로, 새 단지 분석 때마다 내진·소음 키워드 grep을 표준 절차에 넣을 것.
- 내진(건축물대장 BLDRGST_API_KEY)·소음(EIASS, environment_analysis.py, MOLIT_API_KEY)용 전용 모듈 자체는 존재함(building_registry.py, environment_analysis.py)이지만, Claude API 미사용 방침과 별개로 이 모듈들은 "정부 공공API 조회"라서 비용이 아니라 무료키+네트워크 문제임 — 다만 클라우드 세션은 egress 차단이라 PC 등 실제 네트워크 환경에서만 돌릴 수 있음(공공API는 위 최우선규칙의 "Claude API 미사용"과 무관, 필요시 사용 가능).

## 인프라·계정
- GitHub: github.com/kingjin77-rgb/apartment-notice-analyzer ← **이 저장소가 유일한 소스 저장소**
  - PC 로컬 사본: D:\DDownloads\apartment_notice_analyzer
- Claude 계정: hyunjink057 로 통일. 구글드라이브(hyunjink057): 분양공고문_원본 / 검토보고서_완성본 / 법령_원본 3개 폴더
- 완성 보고서 docx는 저장소에 두지 않는다 → 드라이브 검토보고서_완성본으로. (재생성 가능하므로 보관 불필요)
- 법령 원문 PDF → 드라이브 법령_원본. law_mapping.json 의 requires_verification 항목은 PDF 원문 대조 후에만 false 로.

## 분석 우선순위 (2026-08-07 사용자 확정)
- **일반 분양(매매형) 아파트 조사가 민간임대보다 우선.** 민간임대주택법 관련 조사도 계속하되 비중은 낮춤.
- 매매형 기준 미확보 법령(우선순위순): ①주택공급에 관한 규칙(계약금/중도금/잔금 비율, 청약자격 배점표) ②표준공급계약서 매매형(마감재 변경고지, 위약금 비율) ③건축법 시행령(발코니 확장 구조변경기준) — law_mapping.json에 아직 requires_verification: true로 남아있는 4개 카테고리와 직결.
- 확정된 매매형 근거: 주택법 제57조(분양가상한제, 2026-08-07 원문대조), 공동주택관리법시행령 제36조·별표4(하자담보책임, 2026-07-27 대조).

## 파이프라인 구조 요점
- section_splitter.py: 문장 분할 후 law_mapping.json 6개 카테고리 키워드 필터 — **미매칭 문장이 버려지는 구조적 결함 있음 (개선 예정 1순위)**
- toxic_clause.py: RED_FLAG_PATTERNS 정규식 12종 + defect_precedent 병합 (2026-08-06 수리 완료)
- 변경공고 대조 기능 없음 — **개선 예정 2순위** (파라곤3차·광명11·천안이 변경/정정공고 단일 소스 사용 중)
- NotebookLM 연동 폐기 → 구글드라이브로 전환 (RULES.md 1번의 notebook_query 언급은 역사적 잔재)

## 변경 이력
- 2026-08-06 (클라우드 세션): toxic_clause risk_level 배선 버그 수정, red_flags 정규식화, defect_precedent 병합, fire_safety "감지기" 오탐 제거, indoor_air "입회" 오탐·단위없는 숫자 오인식 수정, slide_engine.js JL 스타일 전면 재작성(40→30장), build_slides.js 신규.
- 2026-08-07 (클라우드 세션): 주택법 정상본(52p) 확보·제57조/64조 원문대조→law_mapping.json 분양가상한제 확정. 민특법 제44조(임대료5%상한) 신규 카테고리 확정, 우선분양전환권 카테고리 신규(미확정 표시, 공공주택특별법 제50조의3이 공공사업자 전용조항이라 민간사업자 물건 적용여부 불명확). 실제 JL 제안서 샘플(100p) 확보 → 디자인 방향 전면 정정(사진배경+네이비/골드 톤, 이전 흰배경카드형 폐기). 분양아파트(매매형) 우선순위 확정. Canva 커넥터 연결 제안(대기중). 안양 에버포레 자연&e편한세상 A1BL·A2BL 검토보고서 신규 작성(빌드·verify 통과, git push는 세션 권한 문제로 실패 — bundle 백업 3회 전달). hwp 파일은 이 세션에서 파싱 불가 확인. 품질·스타일 지시사항 13건 수신(목차 삭제, "(계속)" 삭제, 텍스트크기 실측 준수, 동별·세대별 분석 우선, 공통 약관 스킵 등) — 상세 명령 대기중이라 코드 미반영, 위 섹션에 방향성만 기록.
