# 분양공고문 분석 표준 절차서 (ANALYSIS PLAYBOOK)
> 2026-08-08 제정. 신규 단지가 들어오면 **어떤 세션이든 이 절차를 위에서부터 순서대로** 수행한다.
> 목표: 누가 봐도 완벽한 전문가급 검토보고서 — 유형 오판 0건, 모든 주장에 원문 증거, 수치는 실측.

## 0단계 — 자료 접수
- 공고문 원본 PDF를 드라이브 `분양공고문_원본/<단지명>/`에 저장 (변경·정정공고 포함 전부)
- 클라우드 세션: Google Drive `download_file_content`로 PDF 수령 → `pdftotext`로 전문 텍스트 추출 → `/home/claude/<slug>_full.txt` 저장
- **블록 2개 이상이면 무조건 통합본 1권** (공통 + 블록별 장 구성) — CLAUDE.md 고정규칙

## 1단계 — 사업유형 판별 (분석 프레임 확정) ★검수게이트 1
```
python3 report_pipeline/scripts/verify_type_consistency.py <원문텍스트> <slug>   # 유형판별 부분만 먼저
```
- `project_classifier.py`가 유형·근거·확신도 출력 → **미확정(low)이면 대장님 확인 전 진행 금지**
- 판별 유형의 `focus_priority`(data/project_type_taxonomy.json)가 이 보고서의 목차 우선순위가 된다
- 판별 결과·근거 문구를 보고서 표지(또는 1부)에 명시

## 2단계 — 정량 전수검색 (수치 실측)
원문 텍스트에 대해 표준 키워드 세트를 전수 카운트한다. 결과는 모두 "실측 N회" 형태로 인용.
- **면책·이의제기**: `이의[를도]? 제기할 수 없` / `책임[을이]? 지지 않` — 총횟수 + 페이지별 분포(빈도차트용)
- **구조·안전**: 내진, 층고, 영구배수, 필로티, 결로, 스프링클러, 제연, 피뢰침
- **시설·환경**: 문주, 실외기, 중계, 안테나, 쓰레기, 어린이집, 경로당/시니어, 소음, 악취, D.A
- **비용·계약**: 가산비, 확장, 마이너스옵션, 동등 이상, 위약금, 분양보증, 성능등급
- 블록이 여럿이면 **블록 간 카운트 비대칭 표**를 만들어 고지 수준 차이를 잡는다 (에버포레에서 자체 오류 1건·신규 조항 2건을 잡은 방법)

## 3단계 — 심층 분석 (유형별 focus_priority 순서로)
- **가산비 교차검증** (분양가상한제 적용 유형): 산출내역 표 페이지를 렌더해 완전 판독 → 항목별 "계상액 ↔ 실사양 반영 여부" 검증요구 도출. 아이러니 카드(비용 낸 시설이 리스크로 고지된 것) 우선 발굴
- **결합 리스크**: 사업주체 구조(변경·동의간주 조항), 시공사 공개정보(워크아웃 등)와 원문 조항을 결합 — 단정 평가 금지, "보증서 사본·서면 확인 요구" 프레임 (출처 구분 각주 필수, RULES 20번)
- **동별·세대별**: 실제 배치도(공식 홈페이지/홍보물) 확보 → 콜아웃. 경계부 세대(필로티 위층·최상층·지하주차장 상부·시설 인접·저층 PIT) 목록화
- **독소조항**: toxic_clause 정규식 + 에이전트 직접 판독. 각 항목에 **원문 증거카드**(`scripts/make_evidence_card.py`) 부착
- 공통·표준 약관 조항은 스킵(CLAUDE.md 규칙) — 단지 고유 특이조항만

## 4단계 — 시각화 제작 (텍스트보다 시각화 우선 — 최우선 고정규칙)
- HTML 템플릿(`report_pipeline/html_slides/templates/`)에 데이터 주입 → `scripts/capture_html.js`로 캡처
- 표준 세트: ①요약 스코어카드 ②빈도 차트 ③배치도 콜아웃 ④가산비 대시보드(해당 시) ⑤결합리스크 플로우 ⑥증거카드
- 원칙: 화이트 바탕 + 네이비/골드, 추정 그래픽엔 "모식도" 캡션, AI티 금지, 축 단순한 차트만 pptxgenjs 네이티브

## 5단계 — 보고서 생성 (편집가능 PPTX)
```
node report_pipeline/scripts/build_slides_v2.js <slug> <출력.pptx>    # content_v2.js 기반 범용 엔진
```
- 표준 목차: 표지(유형판별 명시) → 통합요약 → 블록별 종합평가 → 동별·위치별(배치도) → 정량분석(차트) → 독소조항(증거카드 포함) → 가산비 검증 → 결합리스크 → 종합 개선요청 권고
- docx 병행 시 기존 `build.js` 사용

## 6단계 — 검수 3단 ★검수게이트 2
```
bash report_pipeline/scripts/run_qa.sh <원문텍스트> <slug> <docx경로>
```
1. 유형-정합성(verify_type_consistency.py): 치명 검출 시 빌드 중단
2. 규칙 검사(verify.js): RULES.md 기계 검사
3. 사람(에이전트) 최종 대조: 스코어카드의 모든 "미기재/누락" 주장을 원문 재검색으로 재확인 — **"없다"는 주장은 두 번 검색 후에만** (에버포레 층고 오류 재발 방지)

## 7단계 — 납품·기록
- SendUserFile로 PPTX(+PDF) 전달, 완성본은 드라이브 `검토보고서_완성본/`으로 (저장소에 두지 않음)
- CLAUDE.md 변경 이력 1줄 + 로드맵 체크 갱신 → 커밋 → (클라우드면 번들 전달, PC pull→push)
- 이번 단지에서 발견한 신규 독소조항 패턴을 toxic_clause/defect_precedent에 누적 (자동학습)
